<h1>Reports#delete</h1>
<p>※以下を削除してもいいですか？</p>
<table>
	<tr><th>Id</th>
		<td><%= @report.id %></td></tr>
	<tr><th>User_ID</th>
		<td><%= @report.user_id %></td></tr>
	<tr><th>Name</th>
		<td><%= @report.user.name %></td></tr>
	<tr><th>Report</th>
		<td><%= @report.report %></td></tr>
	<tr><th>EntryDate</th>
		<td><%= @report.entry_date %></td></tr>
</table>
<%= form_tag controller: "reports", action: "delete" do %>
	<%= hidden_field_tag "id", {value: @report.id} %>
	<%= submit_tag "削除" %>
<% end %>
<a href = "/report">全件一覧へ</a>